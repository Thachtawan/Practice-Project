function findElement(arr, func) {
  let num = 0;
  while (!func(arr[num])) {
    if (num == arr.length) {
      return false, undefined
    } else {
      num++
    }
  }
  return arr[num];
}

findElement([1, 2, 3, 4], num => num % 2 === 0);