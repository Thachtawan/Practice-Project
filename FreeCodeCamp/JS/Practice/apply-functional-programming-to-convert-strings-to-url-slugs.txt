// Only change code below this line
function urlSlug(title) {
  let newStr = title
    .toLowerCase()
    .trim();
  let newArr = newStr.split(/[^a-z]+/ig)
  return newArr.join("-")
}
// Only change code above this line
urlSlug("A Mind Needs Books Like A Sword Needs A Whetstone");